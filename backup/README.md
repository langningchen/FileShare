# Backup Files

This directory contains backup versions of the HTML interface:

- `index-old.html` - Original Bootstrap-based interface (with BootCDN)
- `index-static.html` - Material Design interface with custom CSS (no framework)

The current production interface is built with React + MUI and is located in the `frontend/` directory.
